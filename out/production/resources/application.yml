spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/kcr?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: thfla0901@
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    generate-ddl: true
    properties:
      hibernate:
        format_sql: true
  profiles:
    include: API-KEY  # 추가된 프로파일 설정

logging.level:
  org:
    hibernate:
      SQL: DEBUG
      type:
        descriptor:
          sql:
            BasicBinder: TRACE

# 여기부터 새로 추가하는 설정
openai:
  api:
    token: "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"
  url:
    endpoint: https://api.openai.com/v1/completions
  model: text-davinci-003

