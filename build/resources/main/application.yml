spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/kcr?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: thfla0901@

  jpa:
    hibernate:
#      format_sql: true
      ddl-auto: update #  애플리케이션 실행 시점에 테이블을 drop 하고, 다시 생성함. 개발 서버에서만 사용해야함
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
#    show-sql: true # System.out에 하이버네이트 실행 SQL을 남김
    generate-ddl: true

    properties:
      hibernate:
        format_sql: true

logging.level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE

openai:
  api:
    token: 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
  url:
    endpoint: https://api.openai.com/v1/completions
  model: text-davinci-003


